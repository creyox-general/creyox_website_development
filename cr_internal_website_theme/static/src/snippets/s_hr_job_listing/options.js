odoo.define('cr_internal_website_theme.s_hr_job_listing_options', function (require) {
    'use strict';

    const options = require('web_editor.snippets.options');

    options.registry.HrJobListingOptions = options.Class.extend({
        // Add custom options here if needed in the future
    });
});
